# Memory — expert_study_type

Owner of corpus `Doc/Corpus/E_Study_Type/`.

> Append one entry per substantive task using the convention in `.cursor/agents_memory/README.md`:
> `### YYYY-MM-DD — [task]` with **Task** / **Key output** / **Heuristic learned** / **Gap flagged**.

## Standing notes
- Hub entries: `kim_2025_wgs_breast_landscape` (E4, breast WGS landscape + neoadjuvant pCR substudy), `sztupinszki_2018_scarhrd` / `kim_2020_hrd_wes` (E1, WES/NGS HRD-computation methods that unblock the A1 signal).
- **OCR backlog**: all 3 current E entries are grounded on external/publisher text because the local PDFs are not text-extractable on this host — re-export/OCR needed before upgrading reference entries to decision-grade.
- Known gap: `E6_Sequencing_Platform` has no primary entry (WES/WGS/SNP-array comparability referenced inside E1/E4 entries).
- Most-upstream corpus: its methods feed A_Genomic_Signal (E→A).

## Log
_(none yet)_
