# Proposals

Formal proposals authored by `model_creator` and reviewed by `skeptical_reviewer`.

Each proposal is a file `YYYY-MM-DD_concept_name.md` containing the full proposal package (per `model_creator`'s validation output contract) and a link to its review(s). State is tracked in `Doc/Hypotheses/idea_backlog.md`; rationale is logged in `Doc/Project_history/`.

This folder starts empty.
